# ── Microsoft Fabric / Power BI Service Principal ───────────────────────────
# Same tenant + same service principal as Crio_PowerBI_Agent.
POWERBI_TENANT_ID=
POWERBI_CLIENT_ID=
POWERBI_CLIENT_SECRET=

# ── Skill-Lync Warehouse (Fabric SQL endpoint) ──────────────────────────────
# Same SQL endpoint as Crio (same Fabric workspace), different database name.
SQL_ENDPOINT=twwfdlzo7soexar7f7tzk7wuo4-k6jathn5r3uehhmbl5juoq54da.datawarehouse.fabric.microsoft.com
WAREHOUSE_DB=Skill-lync Warehouse

# Safety: max rows returned per query
MAX_RESULT_ROWS=50000
